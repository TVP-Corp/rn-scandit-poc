{
  "name": "RnScanditPoc",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "react-native run-android",
    "clean": "yarn clean:android && yarn clean:javascript",
    "clean:android": "cd android && ./gradlew clean && cd ../",
    "clean:javascript": "watchman watch-del-all && rm -rf node_modules && rm -rf $TMPDIR/react-* && rm -rf $TMPDIR/haste-* && rm -rf $TMPDIR/metro-*",
    "install:full": "yarn && jetify",
    "jetify": "jetify"
  },
  "dependencies": {
    "jetifier": "1.6.6",
    "react": "18.0.0",
    "react-native": "0.69.10",
    "scandit-react-native-datacapture-barcode": "6.18.2",
    "scandit-react-native-datacapture-core": "6.18.2"
  },
  "devDependencies": {
    "@babel/core": "7.12.9",
    "@babel/runtime": "7.12.5",
    "@react-native-community/eslint-config": "2.0.0",
    "babel-jest": "26.6.3",
    "eslint": "7.32.0",
    "jest": "27.5.1",
    "metro-react-native-babel-preset": "0.70.3",
    "react-test-renderer": "18.0.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
